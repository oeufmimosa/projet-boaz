# dependencies
node_modules
.pnp
.pnp.js

# testing
coverage
playwright-report
test-results

# next.js
.next
out

# typescript incremental cache
*.tsbuildinfo

# production
build
dist

# misc
.DS_Store
*.pem
.vscode

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# local env files
.env
.env*.local

# database
prisma/dev.db
prisma/dev.db-journal

# Uploads : on commit les images pour qu'elles soient déployées sur Vercel
# (le filesystem Vercel est éphémère / read-only à l'exécution, donc tout
# ce qui n'est pas dans le repo n'arrive pas en prod).
# Limite : tout nouvel upload via l'admin en prod ne sera PAS persistant
# tant qu'on n'a pas migré vers Vercel Blob / S3 / R2.

# mailer dev fallback
tmp/

# Claude Code session metadata
.claude/
skills-lock.json

# Markdown (docs internes, prompts, audits, reviews)
*.md
